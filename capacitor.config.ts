import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dipak.visuallearning',
  appName: 'visual-learning-aid',
  webDir: 'dist'
};

export default config;
